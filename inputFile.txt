func duplicar(ref n int) {
    n = n * 2;
}

func sumar(a int, b int) int {
    return a + b;
}

func main() {
    var numero int = 21;
    duplicar(&numero);
    var res int;
    res = sumar(numero, 10);
    println("Resultado final:", res);
}